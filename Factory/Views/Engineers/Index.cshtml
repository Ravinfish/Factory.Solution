@{
  Layout = "_Layout";
}

@using Factory.Models;
@model List<Factory.Models.Engineer>;
<h1>Engineer List</h1>

@if (@Model.Count == 0)
{
  <h3>No Engineers have been added yet..</h3>
}
else
{
  @foreach (Engineer engineer in Model)
  {
    <h4>@Html.ActionLink($"{engineer.Name}", "Details", new { id = engineer.EngineerId })
    </h4>
  }
}